# The local test database is configured directly in docker-compose.yml
# (postgres/postgres/grooming_test) so that the README's psql commands work
# with no setup step. Nothing currently reads this file.
#
# It is kept as the landing place for the extraction pipeline's secrets, which
# is the first part of this project that will need any.

# ANTHROPIC_API_KEY=
